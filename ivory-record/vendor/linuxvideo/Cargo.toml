# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

# VENDORED FOR IVORY. See vendor/README.md for the diff against 0.3.5.
# The workspace root excludes this directory, so it builds standalone.
#
# Upstream's `examples/` and `[dev-dependencies]` are removed: cargo resolves a
# PATH dependency's dev-dependencies into the workspace lockfile even though it
# never builds them, and pulling regex, env_logger and png into ivory's
# dependency surface to support examples we do not compile is not a trade worth
# making. `src/` is untouched.

[package]
edition = "2021"
name = "linuxvideo"
version = "0.3.5"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "V4L2 video capture and output library"
readme = "README.md"
keywords = [
    "linux",
    "video",
    "webcam",
]
categories = [
    "multimedia::video",
    "os::linux-apis",
]
license = "0BSD"
repository = "https://github.com/SludgePhD/LinuxVideo.git"

[lib]
name = "linuxvideo"
path = "src/lib.rs"

[dependencies.bitflags]
version = "1.2.1"

[dependencies.libc]
version = "0.2.172"

[dependencies.log]
version = "0.4.14"

[dependencies.uoctl]
version = "1.0.1"
